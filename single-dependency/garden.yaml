kind: Module

# The type of this module.
type: exec
local: true
# The name of this module.
name: single-dependency
description: common to all REST services

build:
  dependencies: [aggregated-lib]
  command: 
    - "../build.sh single-dependency"
  

